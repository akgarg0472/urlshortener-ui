enum InternalLoaderSize {
  EXTRA_SMALL = 0,
  SMALL = 1,
  MEDIUM = 2,
  LARGE = 3,
}

enum InternalLoaderSpeed {
  FAST = 1,
  MEDIUM = 2,
  SLOW = 3,
}

enum LoaderSpeed {
  SLOW = "slow",
  MEDIUM = "medium",
  HIGH = "high",
}
